#!/bin/bash

cd /root/vsc_projects/PhyforDS/UniKurs_Phy-for-DS/Softmatter
git add . || { echo "Failed to add files"; exit 1; }

current_time=$(date "+%Y-%m-%d %H:%M:%S")
commit_message="Automated commit - $current_time"

git commit -m "$commit_message" || { echo "Failed to commit"; exit 2; }

# Store GitHub credentials in variables
GH_USER="Podaluro"
GH_TOKEN="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"

# Perform the push
git push https://${GH_USER}:${GH_TOKEN}@github.com/LukasLow/UniKurs_Phy-for-DS.git master || { echo "Failed to push"; exit 3; }

